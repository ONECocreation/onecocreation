; npm 12 blocks URL-tarball dependencies by default (EALLOWREMOTE).
; `root` permits exactly the URL deps declared in THIS package.json —
; the house pattern for @pacsarcade/arcade-ui and @pacsarcade/puck-config
; (pinned GitHub tarballs; nothing transitive, nothing from the registry).
allow-remote=root
