# WORK-CLAIM — TASK-121 (THE PINK PASS — all yellow/gold → the popup's soft pink)

CLAIMED-BY: **kimi** (Kimi Code CLI, guest builder lane for Pac)
CLAIMED-AT: 0018.06.16 a₿ (block 965,888)
BRANCH: `feat/task-121-pink-pass`
WORKTREE: `~/dev/worktrees/task-121`
BASE: main tip `642d488` (lint-ignore of the parked .claude worktree; local main is ahead of origin — the house merges locally, this lane never pushes)
ROOM: ONE Cocreation brand pass per Love's List rows 9, 12, 27 — "I want all buttons and pages to look like that popup." Token-name-preserving VALUE swap remapping the gold family onto the rose family (`--gold`, `--gold-deep`, `--gold-2`, `--gold-ink`, `--gold-wash-ink`, `--nav-hover`) in dark + dawn; static button gradients (`.btn-shimmer`, `.tier-btn--a/b/c`) → rose stops; puck-seeds `variant: "gold"` → `"rose"` (values ONLY, no copy); basket glyph 🧺 → rounded-bag inline SVG in rose ink; cartridge-identity harness extended with the rose-on-wash ≥4.5:1 check in BOTH themes plus the `--warn` byte-identity guard. The arcade "gold = money only" law does not apply to Love's cartridge (spec note) — default is ALL rose. Does NOT touch `.env.local`, package.json / lockfile, :3000/:4100 (operator's live processes), or any deployment.
LAW: LANE-CLAIM before building (K5 ruling 1). Commit at gates. Never merge to main, never push. ENGLISH-PIN. BFT dating in comments. Love design laws: no serif faces, contrast ≥ 4.5:1, no color-only meaning. Module law: no cross-app imports.

Files this lane touches (the spec's OWNS list, nothing else):
- `WORK-CLAIM.md` (this claim)
- `src/app/cartridge.css` (gold-family token values, dark block + dawn override; `#classes .lockpill` wash/ink literals)
- `src/app/house.css` (`.btn-shimmer` tint, `.tier-btn--a/b/c` gradients, gold-wash literals paired with remapped tokens)
- `src/lib/puck-seeds.ts` (button `variant` values ONLY — lines ~144, ~193)
- `src/brand/tokens.ts` (gold/goldBright/rose entries + palette p1, values and measured grades)
- `src/components/BasketChip.tsx` (the basket icon component)
- `scripts/cartridge-identity.test.mjs` (spec step 5: the rose-on-wash contrast extension — mandated by the spec itself)

Brief: `~/dev/kimi/inbox/TASK-121-oc-pink-pass.md` (cut 0018.06.16 a₿)
Gates: `npx vitest run` 17/17+ · `node scripts/calendar-view.test.mjs && node scripts/cartridge-identity.test.mjs && node scripts/square-payments.test.mjs` · `npm run lint` = 0 · `npx tsc --noEmit` · `npx next build` · shots of home, /packages, /support, /meditation + the popup in BOTH themes (dev server on :3121 only, killed by recorded PID) + grayscale home reads → `~/dev/kimi/outbox/task-121/shots/` · SUMMARY.md in `~/dev/kimi/outbox/task-121/`
Questions → Number One.
