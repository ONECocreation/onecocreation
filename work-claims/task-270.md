# TASK-270 claim

Lane: home crew (sonnet), branch `feat/task-270`, worktree `~/dev/worktrees/task-270`.
Base at claim: `7b352e1` (main @ T-264 follow-through). Baseline established at cut — see SUMMARY.md
for the verbatim gate outputs.

Scope: ONE Cocreation brand walk L3 — src/lib + src/hooks + src/brand + src/middleware.ts voice.
Per-brief:
1. signoffs.ts/shiplog.ts/console.ts CONSOLE_OFFICERS+CONSOLE_OVERVIEW → per-site config, empty
   default for OC (HB-10, never rename-in-place).
2. nodeconfig.ts CHAT_URL_DEFAULT → OC's own chat door or honest dash; cartridge.ts/earthside.ts
   timeTipUrl → OC's own /api/chain/tip; identity-config.ts NIP05_DOMAIN/SPACE_NAME defaults →
   NEXT_PUBLIC_* derived with OC default, SPACE_ROLES verbs re-worded (keys hold, HB-3).
3. src/brand/cartridges/pacman.ts: `// template lineage` note only (HB-8 is the Admiral's).
4. Bin C docblocks re-voiced across src/lib/**, src/hooks/**, src/brand/**, src/middleware.ts.
5. Tests: officers/seeds empty under OC site config, populated when asked; no default URL in
   src/lib points at frens.earth/pacsarcade.org except sanctioned bft provenance rows.

Bin B (renames, cookies, routes, space keys, package scopes) is HOLD — flagged to Seams only.
See `inbox/TASK-270-oc-brand-walk-lib-voice.md`.
