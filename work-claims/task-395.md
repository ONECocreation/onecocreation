# work-claims/task-395.md

CLAIMED — block 968,132 · builder = the home crew (Sonnet) under Number One's gate.

worktree `~/dev/worktrees/task-395` · branch `feat/task-395-discovery-call-checkout` · base `main @ bdcbc181b128933d672b55e2d27316d10338ce34`.

Brief: `~/dev/kimi/inbox/TASK-395-oc-discovery-call-checkout.md` (as-cut copy: `~/dev/kimi/archive/as-cut/TASK-395-oc-discovery-call-checkout.as-cut.block-968132.md`).

## OWNS

- `work-claims/task-395.md` (NEW, this file)
- `src/components/booking/SlotPicker.tsx` (the discovery-branch changes of Build 2 only — every other line byte-identical)
- `src/lib/cart.ts` (the ONE additive `note?: string` member on `CartLine` `:11-18` — every other line byte-identical)
- `src/app/api/cart/route.ts` (the additive `note` body member `:197-212` + its carriage into the session-line push `:257-261` — every other line byte-identical)
- `src/app/api/cart/checkout/route.ts` (the ONE additive `note:` line in the booking customer `:322-330` — every other line byte-identical)
- NEW `tests/discovery-checkout.test.ts` (test file name fixed before this claim, per the brief's Build 4)

## READ-ONLY and FORBIDDEN

Per the brief: `src/app/api/bookings/checkout/route.ts`, `src/lib/payments.ts`, `src/lib/btcpay-modal.ts`, `src/components/store/CartPanel.tsx`, `src/lib/booking.ts`, `src/app/book/[serviceId]/page.tsx` are READ-ONLY. No new payment rail, no hard-coded price, no new CSS, no widening past the discovery branch (decision A), no civil-date stamp.
