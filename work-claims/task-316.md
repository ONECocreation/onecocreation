# work-claim — task-316 (the sign-in door learns the signer-app handoff)

Lane: Ms. Kimi (sub-agent) or home crew — whoever the K-note names. Base = onecocreation main @ **2ad94ea or newer**
(`git merge main` first: T-315 drops the email field's autoFocus in the same file). Branch `feat/task-316-door-handoff`.
Worktree cut by Number One; `npm ci` first. Lane ports **4402–4405**. GO: `~/dev/kimi/inbox/TASK-316-oc-door-learns-the-signer-handoff.md`.
