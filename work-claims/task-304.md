# work-claim — task-304 ("Send to user" — the studio's guest-door invite letter to one member)

Lane: Ms. Kimi (sub-agent). Base = onecocreation main @ **1223d5d or newer** (T-306 + T-320 landed). Branch `feat/task-304-send-to-user`.
Worktree cut by Number One; `npm ci` already done. Lane ports **4418–4421**. GO: `~/dev/kimi/inbox/TASK-304-oc-send-to-user.md`.
OWNS: NEW `src/lib/mail-studio-invite.ts`, NEW `src/app/api/admin/studio/invite/route.ts`, NEW `src/components/studio-overlay/SendToUserChooser.tsx`, `StudioRoom.tsx` stub only (:265-269), tests, this claim. Guest door only — never the keyed director link; one member per send, never bulk. Stamped 0018.06.26 a₿.
