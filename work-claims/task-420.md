# work-claim — task-420 (OC · S · the seeds carry the basket's words — the support-gifts residue T-411 named)

Lane: the home crew (Ms. Kimi's builder, K108 Ask 2, under Number One's gate). Base =
onecocreation main **57196cbdc68078f182314d1fbceb2bc87e923093** (PR #60 = T-419, carries
PR #59 = T-416; confirmed by `git rev-parse HEAD` in this worktree). Branch
`feat/task-420-seeds-residue`. Worktree cut by Number One at `~/dev/worktrees/task-420`,
`npm ci` done before hand-off. Lane ports **4758–4761**. Brief:
`~/dev/kimi/inbox/TASK-420-oc-seeds-residue.md`; its four tails — `## Cut note — block
968,196`, `## Amendment — block 968,198`, `## Ruling — decision C, block 968,202`,
`## Verify note — block 968,203` — SUPERSEDE wherever they contradict the body. Their
rulings as worked here: the four `cartCheckoutLine` rail states take their words from
`~/dev/briefings/ASTRA-card-gifts-968194.md` §3 (Cut note); `BuyPanel.tsx:266`'s
else-branch sentence becomes "Pay in bitcoin — it travels on-chain, straight to the
artist's own wallet." (Amendment, build 6); **decision C is RULED at block 968,202 —
variant B (word + glyph) ONLY**, the two seed eyebrows become "Support This Work —
Gently" (Ruling); the OWNS of `tests/feature-switches.test.ts` widens to the WHOLE R4
describe `:139-151`, the describe title's stale `:65` pointer re-trued to `:32` in the
same hunk (Verify note).

What the lane builds: the six seed strings (home band 6 eyebrow + rich line, /support
hero eyebrow + two jar-panel lines, /cart blurb), the two pwyc-letters refund lines, the
four `cartCheckoutLine` returns (Astra §3's exact sentences), BuyPanel's bitcoin-only
sentence, the two riding expectations moved with their sources, and one NEW pin file
`tests/seeds-basket-words.test.ts` (variant B) so a reseed can never silently bring the
old words back. A copy-only lane: no CSS, no component, no route shape, no mockup.

## OWNS

`work-claims/task-420.md` (this file, first commit),
`src/lib/puck-seeds.ts` (three regions only: home band 6 `:454-469`, the /support seed's
hero + jars panel `:618-632`, the /cart seed's hero `:1857-1863`),
`src/lib/pwyc-letters.ts` (the `:245-248` hunk only),
`src/app/cart/page.tsx` (`cartCheckoutLine`'s string returns `:32-36` only),
`src/components/store/BuyPanel.tsx` (the bitcoin-only sentence at `:266` only —
amendment, block 968,198),
`tests/feature-switches.test.ts` (the whole R4 describe `:139-151` at base — all four
table rows + the describe title re-trued `:65` → `:32`; Verify note, block 968,203),
`tests/cart-puck.test.ts` (the `:97` expectation only),
`tests/seeds-basket-words.test.ts` (NEW — the pin, variant B).

READ-ONLY (grounding only, never edited): `src/components/TipJar.tsx`,
`src/app/support/page.tsx`, `src/components/sections.tsx`, `src/lib/jars.ts` (411's
merged ground, the wording source), `src/lib/payments.ts` (411's law stands),
`tests/support-jars-basket.test.ts` (411's pin file), `tests/terms-puck.test.ts`,
`tests/jewelry-puck.test.ts`, `tests/media-puck.test.ts`, `src/lib/puck-store.ts`, the
`/style` editor stack, `src/lib/puck-config.tsx`.

## What is NOT in this lane

Never touched: T-429's territory (`src/components/booking/SlotPicker.tsx:701`,
`src/lib/payments.ts:137`); T-422's territory (`BuyPanel.tsx:127-131`,
`feature-switches.test.ts:133-135` and `:218-220` — disjoint hunks in a parallel lane);
the terms/jewelry/media seeds and their live pages; the bookings-change refund letter;
BuyPanel beyond its `:266` sentence; any rail machinery; any CSS file, component, or
route logic beyond the string returns named above. Variant A of the pin is NOT built
(decision C ruled variant B at block 968,202). No sweep of the wider lightning
inventory (Seams, follow-on). No push, no PR, no merge, no fetch, no rebase, no config.
Block-height stamps only.

## Cut note

Stamp per the brief's verify note, block 968,203 (claimed same block — house beacon
`curl -s https://time.pacsarcade.org/height` → 968203).
