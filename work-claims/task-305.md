# WORK-CLAIM — TASK-305 (ONE Cocreation: the studio room gets a key — every link into `onecocreation_studio` carries `&password`, and the guest door arrives both-off)

CLAIMED-BY: Number One (home crew, Claude Fable 5.1), solo lane, no parallel runs.
CLAIMED-AT: 0018.06.25 a₿ (block 967,178 — the GO stamp).
BRANCH: `feat/task-305`
WORKTREE: `~/dev/worktrees/task-305`
BASE: onecocreation main @ `7b6f7bf` — confirmed: `git rev-parse HEAD` = `7b6f7bfc125a41009ca82617762c04ece0e77841` at claim time, matches the brief's cut.
BRIEF: `~/dev/home/inbox/TASK-305-oc-studio-room-keys.md`
SOURCE: T-292 DESIGN.md §4.1 (SECURITY — open rooms, claimable desk) + §3 lane 1 (`~/dev/kimi/archive/task-292/patches/task-292/DESIGN.md`); Love call #4 item 6 (both-off arrival).

## Ground (re-verified at claim, grepped fresh)

- `src/lib/live-links.ts` today: `studioGuestLink(host, room, handle?)` builds `?room=<r>&webcam&mute&label=<label>` — no password, no `&videomute`. `studioDirectorLink(host, room)` builds `?director=<room>&label=Love&muteallguests` — no password (first director to open it claims the desk, fork `main.js:664-665`). `studioVdoLinks(roomPrefix, host)` derives `room = ${roomPrefix}_studio`, `push = ?room=<r>&push=host`, `guest = studioGuestLink(...)`. `studioGuestCameraLink(host, room, handle)` builds `?room=<r>&push=<handle>`. None carry a key today — confirmed by grep, zero `&password` literals anywhere in `src/`.
- Call-sites, re-grepped against the CURRENT tree (line numbers drift slightly from the brief's citation, ground still holds):
  - `src/app/a/studio/page.tsx:69-70` — `const vdo = studioVdoLinks(...)` / `const director = studioDirectorLink(config.meeting.vdoHost, vdo.room)`.
  - `src/app/a/live/page.tsx:37-40` — `const studioVdo = studioVdoLinks(...)` in the page body, `studioDirector={studioDirectorLink(config.meeting.vdoHost, studioVdo.room)}` as a `GoLiveRoom` prop (JSX at line ~42). `go-live-room.tsx` (client) only ever RENDERS the already-built `studioVdo`/`studioDirector` strings (`href={studioDirector}`, `value={studioVdo.push}` etc, lines 384-388/526-531/604-605) — never derives them; prop-threading only, confirmed no client-side key derivation needed there.
  - `src/app/rooms/[slug]/page.tsx:82` — `const studioVdo = studioVdoLinks(switches.meeting.vdoRoomPrefix, switches.meeting.vdoHost);` and `:142` — `cameraDoor = studioGuestCameraLink(switches.meeting.vdoHost, studioVdo.room, mine.slice(...))`.
  - `src/components/rooms/RoomVideoSlot.tsx` — the Stage's own view tiles, hand-built inline (not via a live-links.ts builder today): the host frame `src={\`https://${vdoHost}/?view=host&room=${encodeURIComponent(studioRoom!)}&cleanoutput&autostart&chat=0\`}` and `GalleryTile`'s `src={\`https://${vdoHost}/?view=${encodeURIComponent(handle)}&room=${encodeURIComponent(studioRoom)}&cleanoutput&autostart&chat=0\`}`.
- **View-link raw-vs-hash finding (Ground's own ask):** the fork's `&hash` param (`main.js:3448-3475`) is NOT a password substitute — it's an invite-link convenience that still PROMPTS the human for the password (`promptAlt(getTranslation("enter-password-2"), ...)`, `main.js:3455-3459`) and only checks the typed password's hash against the short CRC in the URL; it never supplies the password itself. The actual signaling topic folds the RAW password into the room hash — `lib.js:27981-27989` (`checkToken`) and `lib.js:28038-28045` (`registerToken`): `var hashedRoom = session.roomid; if (session.password) { hashedRoom += session.password; }` then hashed into the room's real topic — confirmed again in `generateTopic()` (`lib.js:40570-40590`, folds `password` or `hash` into `sensitiveData` the same way). An unattended iframe `view=` tile can never run `promptAlt` (no human at the keyboard), so **the view tiles need the RAW `&password=<key>`**, not `&hash`. `sanitizePassword` (`lib.js:3770-3790`) only `encodeURIComponent`s the value — hex is a strict subset of what survives untouched, so no extra filtering is needed beyond using hex.
- Secret: `SEAT_SECRET` (env name only, never printed). Same HMAC idiom as `src/lib/member-auth.ts:24-28` (`secret()`/`hmac()`), `src/lib/subscribers.ts:136-138` (`unsubscribeToken`), and `src/lib/studio/overlay-token.ts:18-33` (`overlayToken` — the closest precedent: `createHmac("sha256", s).update(\`studio-overlay:${scene}\`).digest("hex").slice(0, 40)`, derive-or-dash on a missing secret). `studioRoomKey(room)` mirrors that exactly, sliced to 12.
- Test-env check: no global `SEAT_SECRET` is set by `vitest.config.ts` or any `.env*` file read in tests — every existing spec that needs it sets/deletes it itself (`grep -rln SEAT_SECRET tests/` confirms the pattern). The new test file follows the same discipline. `scripts/shots-fixture.sh:172` DOES mint a throwaway `SEAT_SECRET="fixture-seat-$(date +%s)-$$"` and exports it to the served app (`COMMON_ENV`, line ~281) — so the shots fixture is KEYED, not the derive-or-dash unkeyed state; the brief's "say which state the shots show" is answered: keyed.
- Existing test pins that hardcode the pre-key link shapes (need updating, "any live-links pin" per OWNS): `tests/go-live-door.test.ts` (3 literal `studioGuestLink`/`vdo.guest` strings), `tests/studio-own-door.test.ts` (1 literal `vdo.guest` string), `tests/studio-desk-doors.test.ts` (the guest blurb text + the `studioDirectorLink(config.meeting.vdoHost, vdo.room)` source-string pin). `tests/stage-shows-the-studio.test.ts` and `tests/named-guest-camera-door.test.ts` render `RoomVideoSlot`/`cameraDoor` WITHOUT a key prop — confirmed their assertions stay byte-identical since an absent key never appends `&password`.

## Seams (found during Ground, not this lane's to fix)

- Threading the key to `RoomVideoSlot.tsx`'s view-link mint requires the key to ride through `src/app/rooms/[slug]/page.tsx` → `ClassroomView.tsx` → `StageView.tsx` → `RoomVideoSlot.tsx`. `ClassroomView.tsx`/`StageView.tsx` are NOT in this lane's OWNS list, but they are the only pass-through path — the same pattern `vdoHost`/`studioRoom` already ride. Minimal, additive, optional-prop edits only (`roomKey?: string`, forwarded unchanged, byte-identical when absent) — flagged here per the brief's "make the minimal edit and flag it in Seams" instruction.
- OBS scene links on the VPS (T-237's run sheet) mint `&website=<url>` studio-source URLs that publish INTO the same room (`src/app/a/studio/page.tsx`'s `showInStudioUrls`, `?room=<r>&website=<overlayUrl>&push=scene`) — these now need the same `&password=<key>` appended once the room carries one, or OBS's scene source lands in the wrong (unkeyed) room the moment `SEAT_SECRET` is set in production. This lane does NOT touch `showInStudioUrls` (outside OWNS, and it isn't one of the four call-sites the brief names) — flagged for the run-sheet owner, not fixed here.

## OWNS (per brief, verified against current tree)

`src/lib/live-links.ts` (additive `key?`/`studioViewLink`), `src/lib/live.ts` (`studioRoomKey` + re-export), `src/app/a/studio/page.tsx`, `src/app/a/live/page.tsx`, `src/app/a/live/go-live-room.tsx` (prop threading only — verified none needed, it already only renders pre-built strings), `src/app/rooms/[slug]/page.tsx`, `src/components/rooms/RoomVideoSlot.tsx` (view-link mint only), `src/components/studio-overlay/StudioRoom.tsx` (blurb + key line only), tests: `tests/studio-room-keys.test.ts` (new) + `tests/studio-desk-doors.test.ts`, `tests/studio-links-top.test.ts`, `tests/go-live-door.test.ts`, `tests/studio-own-door.test.ts` (existing pins, string updates only), `work-claims/task-305.md`. Plus the minimal Seam edits to `ClassroomView.tsx`/`StageView.tsx` (additive `roomKey?` pass-through only), documented above. NOT: `src/lib/letters.ts` (T-304), `src/app/meet/**` (T-297), the fork, mail, anything else.

LANE-DONE pending build + gates + shots + commit.
