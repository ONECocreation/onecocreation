# TASK-281 claim

Owner: home crew (sonnet)
Base sha: 6ca5244
Branch: feat/task-281
Ruling: H108 A, 0018.06.25 a₿ · block 967,125

RULING on ## Seams: option (a) dual-listing `page-store` under both scopes is NOT
taken — rename the two `file:` packages cleanly; `src/components/PuckEditor.tsx:7`
stays untouched by this lane, its one import line becomes Number One's
minimal-forced-edit at merge. The build WILL be red in this worktree on that one
line until then — proven with the exact error, then proven green with a throwaway
local edit that is reverted before LANE-DONE.

## OWNS
- `package.json` (the two renamed dependency entries)
- `packages/operator-auth/package.json`
- `packages/page-store/package.json`
- `package-lock.json` (regenerated by `npm install`, not hand-edited)
- `src/lib/operator-auth.ts`
- `src/lib/puck-store.ts`
- `next.config.ts` (the two entries in the `transpilePackages` array only)
- `work-claims/task-281.md`

## NOT-OWNS (explicit)
- `src/app/globals.css` — the two `arcade-ui` lines stay untouched.
- `vitest.config.ts` — no edit expected, verify and say so.
- All six tarball packages' names/URLs in `package.json` — cannot be
  re-scoped by this lane (out of reach, upstream republish required).
- Every import of a TARBALL package (`arcade-ui`, `plugin-rails`,
  `presence`, `puck-changelog`, `puck-config`, `variant-engine`).
- `src/components/PuckEditor.tsx` — Ms. Kimi's live lane, out of reach.
  `src/components/style/BrandBoard.tsx` — also Kimi-owned.
- `src/lib/puck-seeds.ts`, `src/lib/puck-blocks/parallax-band.tsx` —
  comment-only mentions of `@pacsarcade/puck-config`, harmless, left stale.

This is the eighth and LAST lane of the 274→281 chain.
