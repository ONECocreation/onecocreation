# TASK-231 — claim (0018.06.24 a₿ · block 967,070)

Ms. Kimi's sub-agent, working `feat/task-231` off main @ 7b352e1. Building
/retreats as a designer page: a Puck seed transcribed from today's hand-built
JSX (hero, lead, empty-state line) with the live retreat card grid as ONE
data-bound block, `RetreatsList`, registered in puck-config and mirrored in
copilot; the page reads Puck first and falls back to today's JSX until Love
publishes. OWNS: src/app/retreats/page.tsx, src/lib/puck-seeds.ts,
src/lib/puck-blocks/retreats-list.tsx (NEW), src/lib/puck-config.tsx,
src/lib/copilot.ts, one new test file. Never the live site, never the vault,
never push.
