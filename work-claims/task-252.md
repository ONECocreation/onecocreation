# work-claim — TASK-252: the home page's "My Story" wears Love's new picture (OC, small)

LANE: home crew (sonnet) · worktree `~/dev/worktrees/task-252` · branch `lane/task-252` · base main @ `45187c9`
(T-251 merged). OWNS: the `About()` block in `src/components/sections.tsx` (~139-147), `src/brand/cartridge.ts`
(`portraits.headshot`), `src/brand/cartridges/earthside.ts` (`portraits.headshot`, same person's slot — read-verified),
the stale seam comments at `src/lib/puck-seeds.ts:78-79` and `src/app/about/page.tsx:114-117`, and the new test
`tests/home-my-story-picture.test.ts`. Do NOT touch `src/components/store/**` or the store rules in house.css
(T-253's lane), `Packages()` in sections.tsx / `src/app/packages/**` / `.shine-hover`/`.feat` rules (T-254's lane),
or `/welcome`'s hands slot (`WelcomeFlow.tsx:30-32` — waits for a different photo).

Date stamps: 0018.06.24 a₿.
