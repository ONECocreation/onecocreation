# work-claim — TASK-254: the memberships cards stand level, and the tier page's picture keeps one shape (OC, small)

LANE: home crew (sonnet) · worktree `~/dev/worktrees/task-254` · branch `lane/task-254` · base main @ `45187c9`.
OWNS: `src/components/sections.tsx` (`Packages()` ~190-285), `src/app/packages/[slug]/page.tsx`, the
`.shine-hover` / `.feat` / `.tier-name-pill` rules in `house.css` (add new rules, do not rewrite shared ones),
`tests/packages-cards-level.test.ts` (NEW). Do NOT touch: `About()` in `sections.tsx` (~139-147) or
`src/brand/cartridge.ts` (T-252); `src/components/store/**` or the `.card`/`.flip-card`/`.item-flip`/
`.flip-scroll`/`.card-title`/`.card-sub`/`.push` rules in `house.css` (T-253); button classes/words (T-255).
Stamp: 0018.06.24 a₿.
