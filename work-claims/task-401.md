# work-claims/task-401.md — task-401 (ONE Cocreation: "you're in as" reads once, not on every door)

Lane: the home crew (Sonnet builder, under Number One's gate). Base = onecocreation main
`ca2b692d25d88533bfea593ef7873237aa8021b2` (PR #48 on #47 — TASK-399's merge — on `11ef2f4`),
cut at block 968,141. Branch `feat/task-401-you-are-in-as-once`. Worktree
`~/dev/worktrees/task-401` (cut by Number One), `npm ci` already done. Lane ports **4690–4693**.
Brief: `~/dev/kimi/inbox/TASK-401-oc-you-are-in-as-once.md`.

Claimed at block 968,142 (house beacon, fresh read at claim time).

## OWNS

- `work-claims/task-401.md` (NEW, this file)
- `src/components/rooms/PackageRoomsCard.tsx` — the `signedIn && name` block (`:161-165`), the
  `name` prop + its comment (`:53-55`), the TASK-183 docblock paragraph (`:22-35`) — every other
  line byte-identical
- `src/components/rooms/RoomsShelf.tsx` — the `:80` mount prop (`name={feed.handle}` dropped) and
  the `:76-79` comment only — the foot note `:85-89` byte-identical
- `tests/classes-cards.test.ts` — the name-line pins rewritten to the new truth; every pin that
  never involved the name byte-identical

## READ-ONLY (grounding, never edited)

`src/components/rooms/CircleView.tsx` (decision B — zero mentions today, none owed), `src/app/house.css`
+ `src/app/cartridge.css` (TASK-398's — the `.room-card-name` dead rules are a named seam, not an
edit here), `src/lib/matrix-rooms.ts`, `tests/rooms-shelf.test.ts` (never pinned the line), every
page file, `src/components/sections.tsx` + `src/lib/puck-seeds.ts` + `tests/package-names.test.ts`
(TASK-394's), `scripts/fixture-kv.cjs` + `scripts/fixture-kv.test.mjs` (TASK-399's).

FORBIDDEN (per brief): any new once-mention surface (no header chip, no badge); any `style={{}}`
addition; touching the foot note's words; a compat shim that keeps `name` accepted "just in case"
(a dropped prop is dropped — swept, not deprecated); any civil-date stamp.

Block height at claim: 968,142.
