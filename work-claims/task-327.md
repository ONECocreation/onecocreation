# work-claim — task-327 (/style wears the console's own header, with a door back to /a)

Lane: Ms. Kimi (sub-agent). Base = onecocreation main @ **864b9e4**. Branch `feat/task-327-style-header`.
Worktree cut by Number One; `npm ci` done at cut. Lane ports **4454–4457**. GO: `~/dev/kimi/inbox/TASK-327-oc-style-unified-header.md` (cut 0018.06.26 a₿).
The Admiral's ask: "make sure they have a unified header so we can get back easily to the admin area" — all three /style routes (catch-all, brand board, reference viewer) mount the same SiteChromeHeader Love sees on /a, plus a slim room strip ("Style" + the page being edited + ← Back to admin + View site); the Puck canvas keeps full height below via the one named seam (PuckEditor root 100vh → 100%).
K51 advisory on file: `~/dev/briefings/bench/astra-oc-admin-usability/review-r2-style.md` §5 (header contract) — the brief rules where they differ.
