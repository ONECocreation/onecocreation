# work claim — task-233

Claimed 0018.06.25 a₿ (block 967,125) by Ms. Kimi's lane crew.

Lane: /style publish legibility — publish words in plain language, the preview overlay
becomes Live → Draft before/after per theme, the light preview pane derives from
`html[data-oc-theme="light"]` truth instead of the `.oc-pv-light` twin, drifted-comment
sweeps, first-ever publishAll + GET `{draft,live}` + overlay + derivation-source pins.

OWNS: `src/components/PuckEditor.tsx`, `src/app/style/preview.css`,
`src/components/PaletteVars.tsx` (scopes only), one-line comment fixes in `scar.css` +
`packages/page-store/src/store.ts`, tests.

NOT: `cartridge.css` / `cartridges.css` (read-only truth — flag-and-stop if a change
proves needed), `src/components/style/PagesPanel.tsx` (T-230), `src/lib/lint.ts` semantics.
