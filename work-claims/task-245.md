# work-claim — TASK-245: the Heart Field stage shows Love's studio picture, the others in a gallery below

LANE: home crew (sonnet) · worktree `~/dev/worktrees/task-245` · branch `lane/task-245` · base main @ `05fa46d` (T-243 + T-246 merged). OWNS: `src/components/rooms/RoomVideoSlot.tsx` (whole file), `src/app/rooms/[slug]/page.tsx` (the props lines only), `tests/stage-shows-the-studio.test.ts` (NEW). Seams (adjacent, unowned, minimal diff): `src/components/rooms/StageView.tsx` and `src/components/rooms/ClassroomView.tsx` (thread `rail`/`vdoHost`/`studioRoom`/`onCameraMxids` down to the slot), `src/components/rooms/classroom.css` (the `.cl-stage-gallery`/`.cl-gallery-tile` rules the gallery wears), `tests/classroom-live.test.ts` (one pinned import-line assertion updated to match the room page's new import).
