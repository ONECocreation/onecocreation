# TASK-232 — claim (0018.06.25 a₿ · block 967,125)

Ms. Kimi's sub-agent, working `feat/task-232` off main @ ec703a4 (the T-231
merge — the RetreatsList registry pattern is already in the tree). Building
/packages (the memberships index) as a designer page: a route-level
memberships-switch gate FIRST (T-187 idiom — the switch lives inside
Packages() today and a naive Puck branch would bypass it), a Puck seed with
Love's words (kicker, heading, lead + PACKAGE_DOORS_WORDS, the gate-explainer
footnote), and the three-tier grid as ONE data-bound block, `PackagesGrid` —
names and prices never enter the seed; they stay in TIERS and render through
the block with the switch-driven doors (rails-ON "See the package" / rails-OFF
waitlist, the pinned packageWaitlistProps contract). The page reads gate →
Puck → hand-built fallback; sections.tsx is NOT edited. OWNS:
src/app/packages/page.tsx, src/lib/puck-seeds.ts,
src/lib/puck-blocks/packages-grid.tsx (NEW), src/lib/puck-config.tsx,
src/lib/copilot.ts, tests (route-gates extension + one new pin file). Named
git adds only, never -A. Never the live site, never the vault, never push.
