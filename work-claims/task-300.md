# WORK-CLAIM — TASK-300 (ONE Cocreation: /a/studio — Open buttons, the room's name on the card, quick links first, director-only clutter collapsed)

CLAIMED-BY: Number One (home crew, Claude Fable 5.1), solo lane, no parallel runs.
CLAIMED-AT: 0018.06.25 a₿ (block 967,174 — re-verified at claim: `curl https://mempool.space/api/blocks/tip/height` → 967174, matching the GO stamp).
BRANCH: `feat/task-300`
WORKTREE: `~/dev/worktrees/task-300`
BASE: main @ `57bba3a` (T-293 merged) — confirmed: `git rev-parse HEAD` = `57bba3aa2cf067a46fab59c4f7d438a83c6d5252` at claim time, matches the brief's cut.
BRIEF: `~/dev/home/inbox/TASK-300-oc-studio-links-open-buttons.md` (+ source: `briefings/love-call-0018.06.25-studio-walk-notes.md` findings 1-2).

## Ground (verify at cut — one correction from the brief's own text)
- `src/app/a/studio/page.tsx` (101 lines) is a thin Server Component: `headers()` for the operator cookie, `getStudioDoc()`/`getSiteConfig()`, derives `vdo` (`studioVdoLinks`) and `director` (`studioDirectorLink`) from `@/lib/live` (T-261), then renders exactly one child: `<StudioRoom initial=... overlayUrls=... vdo=... director=... showInStudioUrls=... showTitleFallback=... />`. It carries **zero** of the page's visible JSX.
- **The brief's Ground line "`src/app/a/studio/page.tsx` — the links card … sits at the bottom" is not literally where the card lives.** The links card (T-261's three-row VDO links section), the guest panel, the timers/full-scenes fields, and the scene/overlay URL list are ALL inside `src/components/studio-overlay/StudioRoom.tsx` (374 lines, `"use client"`) — the one client half of this route. Confirmed: `grep -rn "StudioRoom" src` shows exactly ONE caller of that component (`page.tsx`) — it is a page-private implementation file, never shared, same role T-261's own claim gave it ("`src/components/studio-overlay/StudioRoom.tsx` (links card only)" was explicitly in T-261's OWNS for this identical card).
- Because the brief's OWNS lists only `page.tsx` (+ one test file + the claim), and every interactive element this brief asks for (an OPEN anchor needs no JS, but the Copy button and the accordion toggle both need `onClick`/state, which a Server Component cannot carry, and cannot be added to `StudioRoom.tsx`'s render without editing that file) — the build is not reachable from `page.tsx` alone. Following T-261's own precedent for this exact card, this lane extends its practical OWNS to include `src/components/studio-overlay/StudioRoom.tsx`, narrowly: the links-card JSX (relocated + Open buttons added) and wrapping "everything else" in one collapsed section. No other file is touched. `StudioRoom.tsx` has no other caller, so this cannot collide with T-285-C (package scope) or T-301 (the operator gate at `OperatorGate.tsx` / `/a` signed-out door — confirmed disjoint by reading `~/dev/home/inbox/TASK-301-oc-operator-key-button-honesty.md`).
- The room's plain name ("Heart Field · the studio") lives in `brand/rooms.json` on the fork (`~/dev/apps/onecocreation-studio/brand/rooms.json`, TASK-262, read-only reference — NOT this repo, a separate deployment/repo). This app has no route to that file, so `page.tsx` carries it as a small hardcoded constant (same idiom as `showTitleFallback: cartridge.copy.productName` already threaded down today), with a comment citing the source.
- House buttons confirmed in `src/app/house.css`: `.btn` (primary CTA), `.btn-sm` (`padding:12px 24px`), `.btn-ghost` (secondary). `SectionHead` (`@/components/console/glass`) is the house pill+rule idiom. The Open-button pattern (`<a target="_blank" ... className="btn btn-sm">Open your director's desk</a>` / `Step on camera`) already exists verbatim in `src/app/a/live/go-live-room.tsx:384-388` (T-261's Next row) — this lane's OPEN buttons match that house idiom, not a new one.
- Existing test `tests/studio-desk-doors.test.ts` (T-261, not owned by this lane, left untouched) pins the three row headings ("your director's desk", "on camera", "a guest's door"), their blurbs, their Copy labels, and their order — the redesign below keeps every one of those strings verbatim (only ADDS the OPEN anchor beside each), so that suite keeps passing unmodified.
- The T-230 accordion idiom (`src/components/style/PagesPanel.tsx:55-92`, the Archive group): `useSyncExternalStore` + a `localStorage` key/event pair, closed-by-default `getServerSnapshot`. This lane's "For the director's desk" section reuses the identical shape under its own key.

## Build (as scoped)
1. `src/app/a/studio/page.tsx`: add `roomTitle` (the hardcoded plain name, sourced-commented) and pass it to `StudioRoom` as a new prop.
2. `src/components/studio-overlay/StudioRoom.tsx`: the VDO-links section (T-261) becomes the FIRST section on the page, titled `${roomTitle} — ${vdo.room}`; each of the three existing rows (heading + blurb kept verbatim) gains an OPEN anchor (`target="_blank" rel="noopener"`, primary `.btn`) beside its existing Copy button (kept, same label); a new line + disabled ghost button ("Send to user — coming with T-304") sits under the card. Everything else (intro copy, error/note, Scene picker, The stage, The full scenes, Guest roster, Overlay URLs) moves under one collapsed section, "For the director's desk (moving to the director view)", closed by default, remembered in `localStorage` (T-230 idiom, new key). Nothing deleted or renamed.
3. New test file `tests/studio-links-top.test.ts`: the card renders before the Scene section; the three OPEN anchors carry `target="_blank"`/`rel="noopener"` and the three builder URLs; the placeholder button is `disabled`; `page.tsx` computes and passes `roomTitle`.
4. Shots per the brief.

## OWNS (as claimed, with the one documented extension above)
`src/app/a/studio/page.tsx`, `src/components/studio-overlay/StudioRoom.tsx` (links-card + collapse wrapper only — extension, documented above), `tests/studio-links-top.test.ts` (new), `work-claims/task-300.md`. NOT `src/lib/live-links.ts`, NOT `src/app/a/live/**`, NOT the fork, NOT `tests/studio-desk-doors.test.ts` (left passing, unmodified).

LANE-DONE pending build + gates + shots + commit.
