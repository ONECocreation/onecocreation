# WORK-CLAIM — TASK-320 (ONE Cocreation: retire the merge queue — Action Items / Bug Testing / Sign-offs board is SCAR·LET arcade fork residue, gone from the client site)

CLAIMED-BY: Ms. Kimi fleet lane runner, solo lane. T-319 (calendar) runs in parallel on forbidden-disjoint files.
CLAIMED-AT: 0018.06.26 00:20 a₿ · block 967,250.
BRANCH: `feat/task-320-retire-merge-queue`
WORKTREE: `~/dev/worktrees/task-320`
PORTS: 4426–4429
BASE: onecocreation main @ `864b9e4` — `git rev-parse HEAD` matches the brief's cut at claim time.
BRIEF: `~/dev/kimi/inbox/TASK-320-oc-retire-the-merge-queue.md` — deletion lane: `/a/action` and `/a/testing` 404 outright; no "Action Items"/"Bug Testing"/"Sign-offs" wording survives in `/a`'s console.

Per the brief: five core deletions + the orphaned `src/app/api/admin/merges/route.ts` + the `console.ts` "duty" registry entry + the four nav-link fixes. `DeployPanel.tsx` / `src/lib/deploy.ts` / `src/app/api/admin/deploy/route.ts` STAY (the brief's correction — they back the live SHIP door). Named nodeconfig decision (a)/(b) and the 27→25 page-count drop will be stated in the SUMMARY.

LANE-DONE pending build + gates + shots + commits.
