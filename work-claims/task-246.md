# work-claim — TASK-246: the stage's video frame stands full height and wears our mark

LANE: home crew (sonnet) · worktree `~/dev/worktrees/task-246` · branch `lane/task-246` · base main @ `3554b9f`. OWNS: `src/components/booking/JitsiRoom.tsx`, `src/components/rooms/RoomVideoSlot.tsx` (the embed block only), `tests/stage-video-frame.test.ts` (NEW). Seam (adjacent, unowned, minimal diff): `src/components/rooms/classroom.css` (the `.cl-stage-embed` min-height rule the slot's new class needs).
