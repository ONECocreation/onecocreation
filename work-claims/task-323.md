# work-claim — task-323 (the admin nav follows Love's day — reorder + "Services" → "Sessions & hours")

Lane: Ms. Kimi (sub-agent). Base = onecocreation main @ **864b9e4 or newer**. Branch `feat/task-323-admin-nav`.
Worktree cut by Number One; `npm ci` already done. Lane ports **4438–4441**. GO: `~/dev/kimi/inbox/TASK-323-oc-admin-nav-order-and-names.md`.
OWNS: `SiteConsoleShell.tsx` (SITE_LABELS / rooms order / SITE_BLURBS), `src/app/a/page.tsx` line 113 only, `tests/console.test.ts` order+label pin, this claim. Presentation-only — `src/lib/console.ts` stays READ-ONLY (T-320 seam). Stamped 0018.06.26 a₿.
